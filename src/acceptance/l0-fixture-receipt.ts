// Pure, side-effect-free L0 fixture receipt contract.
// Imported by both acceptance-bootstrap.ts (writer) and l1-harness-run.ts (reader).
// This module has no top-level executable code — safe to import from any context.

export const L0_FIXTURE_RECEIPT_FILENAME = 'l0-fixture-receipt.json'
